{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'uvicorn src.main:app --host 0.0.0.0 --port ${PORT:-9001}'"
  }
}
